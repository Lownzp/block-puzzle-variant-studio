# 视频变体重建验收标准

## 硬性门槛

1. 按源帧率读取全部视频帧，时间戳误差不超过 1 帧。
2. 自动识别棋盘边界及行列数；稳定状态中每个格子的占用结果必须与人工真值完全一致。
3. 每轮三个槽位的方块形状、实际选择槽位、落点和顺序必须完全一致。
4. 每一步必须通过规则模拟：将识别形状放入识别落点后，得到的视频棋盘状态必须精确一致。
5. 清屏、预设装载和动画变化都必须完成分类；未解释稳定变化数必须为 0。
6. 有任何歧义时必须停止自动生成并标记待复核，不能用低置信结果继续回放。
7. 编辑器复刻视频的步骤数、形状、槽位、落点和清屏顺序必须与步骤 JSON 完全一致。
8. 最终视频输出为恒定 60fps；语义帧时序误差不超过 1 帧，画面无丢帧、破帧或错误点击。

## 分阶段放行

- 识别通过：规则验证动作数等于人工真值动作数，且未解释稳定变化数为 0。
- 回放通过：编辑器输出视频重新送入识别器后，得到与输入步骤 JSON 完全相同的动作序列。
- 变体通过：只改变背景或方块资源，重新识别出的形状、槽位、落点、清屏和步骤顺序保持不变。

## 当前基准样本

- 视频：`F:\4月29日(1).mp4`
- 棋盘：8 x 8
- 源帧率：30fps
- 总帧数：380
- 人工确认动作数：12
- 基准真值：`G:\爆款素材变体生成器\校准\4月29日-基准真值.json`
