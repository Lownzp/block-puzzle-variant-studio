# 视频动作识别优化实施计划

## 1. 目标与范围

目标：让 `G:\爆款素材变体生成器\测试素材` 中的全部视频都能完成棋盘确认、动作识别、人工复核、规则重算、客户端回放和视频验收。

当前场景范围：

- 棋盘在单段玩法中位置基本固定。
- 棋盘为无明显透视的规则矩形网格。
- 棋盘规格不写死，可为 8x8、10x10 或其他行列数。
- 底部主要为左、中、右三个出块槽位。
- 支持彩色块、纯色块、粉沙块、同色相连块、全消、连续消除和片尾。
- 第一版数据集只覆盖当前编辑器可复现的经典三槽拼图玩法。
- 第一版明确排除实拍真人或明显手部实拍、俄罗斯方块下落玩法，以及包含动物、木箱、礼盒等特殊机制的活动新关卡；这些场景等待编辑器扩展后另建数据集。
- 暂不处理明显镜头运动和透视棋盘。

## 2. 强制开发回归集

> 数据集角色说明：以下 6 条视频已经参与过问题定位、算法修改和阈值调整，因此属于开发回归集，不再作为衡量泛化能力的封存测试集。它们仍然必须 6/6 通过，但最终算法验收还必须通过第 16 节定义的独立调优集和封存测试集。

以下 6 条视频全部为强制测试，不允许挑选性通过：

| ID | 测试素材 | 分辨率 | 帧率 | 时长 | 重点覆盖 |
|---|---|---:|---:|---:|---|
| T01 | `2677_@李心怡_#编辑器自制_箭头大块全消_粉沙块_带片尾.mp4` | 720x1280 | 60fps | 27.27s | 粉沙块、箭头大块、全消、片尾 |
| T02 | `2677_@杨金月_#编辑器自制_2d游戏玩法天蓝纯色对称布局.mp4` | 1080x1920 | 30fps | 32.30s | 纯色同色块、对称布局 |
| T03 | `2677_ssq_20251020_彩块游戏玩法快节奏1080-1920.mp4` | 1080x1920 | 25fps | 34.56s | 彩块、快节奏、非30fps |
| T04 | `2677_ssq_20251020_粉色大全消游戏玩法.mp4` | 1080x1920 | 24fps | 58.04s | 粉色同色相连块、大面积全消 |
| T05 | `2677_ssq_20251020_连胜录屏1.mp4` | 360x640 | 24fps | 40.79s | 低分辨率、录屏、连胜过程 |
| T06 | `2677_yjy_20251119_编辑器自制_2d游戏玩法纯色蓝块全消结尾.mp4` | 1080x1920 | 30fps | 29.93s | 纯色蓝块、全消、结尾 |

开发基准 `F:\4月29日(1).mp4` 继续保留，但不能代替上述 6 条回归素材。

## 3. 全集通过的定义

每条测试素材必须依次通过以下五层：

1. **识别通过**：棋盘、行列数、稳定状态和动作候选完整生成，不允许静默输出 0 步。
2. **确认通过**：所有多解、槽位不确定和消除不确定项均能在页面完成确认。
3. **规则通过**：从初始棋盘重新模拟全部动作，所有步骤无越界、重叠或状态冲突。
4. **回放通过**：改造客户端按最终确认脚本完成全部步骤，每步棋盘校验一致。
5. **视频通过**：最终视频比例正确、恒定 60fps、无异常停顿，反向识别结果与最终脚本一致。

只有 6/6 素材五层全部通过，项目验收才算完成。

## 4. 阶段 0：建立人工真值

### 工作内容

- 为 T01-T06 逐条确认棋盘范围、行数和列数。
- 逐步记录放置前棋盘、槽位、完整方块形状、落点、消除开关和消除后棋盘。
- 标记棋盘切换、全消、片尾开始时间和非玩法画面范围。
- 每步保存放置前、动作中、放置完成、消除后证据帧。
- 输出版本化人工真值 JSON，不直接修改历史版本。

### 阶段门槛

- 6 条素材均有人工真值。
- 真值动作数、最终棋盘和消除顺序经过人工复核。
- 没有真值的素材不得计入算法通过率。

## 5. 阶段 1：棋盘定位与人工校准

### 自动定位

- 从多个时间点寻找候选棋盘，不再依赖单帧最大矩形。
- 根据均匀横纵网格自动推测行数和列数，不写死 8x8。
- 使用多帧投票选择位置、尺寸和网格规格最稳定的候选。
- 拒绝贴近整帧边缘、几乎覆盖全屏、下方无出块空间和低置信度候选。

### 人工兜底

- 自动定位失败时进入人工校准，不继续动作分析。
- 用户框选左上角和右下角，并输入行数、列数。
- 页面显示网格覆盖预览，确认后重新扫描。
- 人工配置只绑定当前任务；作为模板复用前必须重新验证。

### 阶段门槛

- T01-T06 均能自动定位或通过人工校准进入下一阶段。
- 棋盘边界误差不超过单格尺寸的 5%。
- 行列规格与人工真值完全一致。
- 不允许再出现“整张竖屏被当作棋盘后继续输出 0 步”。

## 6. 阶段 2：同色出块拆格与多帧恢复

### 工作内容

- 不再使用“一个连通区域等于一个方块格”的单一规则。
- 综合格距、内部边缘、亮度周期、角点和阴影拆分同色相连方块。
- 对左、中、右槽位进行多帧投票，过滤拖动瞬间和手指遮挡。
- 某槽位正在拖动时，从动作前后的稳定帧恢复原始形状。
- 保存每个槽位的候选形状、证据帧和置信度。

### 阶段门槛

- T02、T04、T06 的纯色相连块可以按格拆分。
- T01 的粉沙块和特殊大块形状可以完整表达。
- T03 彩块识别不得因新算法退化。
- T05 低分辨率素材不能因固定像素阈值失效。

## 7. 阶段 3：多阶段证据帧

### 工作内容

- 每步尝试保存放置前、动作中、放置完成和消除后四类帧。
- 缺失阶段必须明确标记，不允许拿其他画面代替。
- 片尾和非玩法帧单独分类，不计入棋盘动作。
- 最后一步继续分析到玩法结束或片尾开始。

### 阶段门槛

- 每个确认动作至少具有放置前和放置后证据。
- 有消除的动作必须具有消除依据或标记待确认。
- 动作证据与人工真值时间误差不超过 1 个源视频帧。

## 8. 阶段 4：双向候选与约束求解

### 工作内容

- 方向 A：从底部候选形状尝试所有合法落点。
- 方向 B：从棋盘前后变化推导新增形状和候选落点。
- 将槽位、形状、落点、消除开关组合后执行规则模拟。
- 放置后立即消除时，使用中间帧和规则约束恢复被消除的形状部分。
- 候选必须同时解释放置前、放置后和消除后棋盘。

### 多解规则

- 只有一个合法候选：自动确认。
- 多个合法候选：全部保留并转人工选择。
- 没有合法候选：显示失败步骤和原因，不得丢弃为 0 步。

### 阶段门槛

- 自动确认步骤必须是唯一合法解。
- 自动确认的槽位、形状、落点和消除开关与人工真值 100% 一致。
- 所有非唯一情况均进入待确认，不允许自动猜测。

## 9. 阶段 5：动作校对、级联重算与版本

### 工作内容

- 页面展示四类证据帧、槽位、形状、落点、完整行列、消除状态、候选数和依据。
- 支持修改槽位、形状、落点和消除开关。
- 修改任一步后，从该步开始重新模拟到最后一步。
- 后续失效步骤明确标红，全部恢复有效前锁定视频生成。
- 保存识别初稿、人工确认版本和最终确认版本，支持回退。

### 阶段门槛

- T01-T06 的待确认项均能在页面完成处理。
- 修改早期步骤时，后续冲突可被准确定位。
- 未通过级联重算的任务无法调用回放接口。

## 10. 阶段 6：模板复用保护

### 工作内容

- 模板仅作为候选，不按文件名或分辨率直接套用。
- 复用前验证视频尺寸、棋盘位置、行列规格、网格对齐和槽位区域。
- 验证失败时回到自动检测或人工校准。
- 在任务记录中保存模板来源和验证结果。

### 阶段门槛

- 错误模板不能进入动作分析。
- 使用模板后的棋盘结果仍需满足阶段 1 的同一验收标准。

## 11. 阶段 7：确定性回放与视频验收

### 工作内容

- 客户端只读取最终确认版本。
- 每步执行指定槽位、形状、落点和消除开关。
- 每步比较完整棋盘位图，失败立即终止。
- 原始录制检查帧时间戳，之后等比输出恒定 60fps 视频。
- 将输出视频重新送入识别器，与最终动作脚本反向比较。

### 阶段门槛

- T01-T06 客户端执行步骤数与最终脚本一致。
- 每步棋盘校验全部通过。
- 输出画面比例正确，无拉伸。
- 最大异常视频帧间隔不超过 0.5 秒。
- 反向识别出的动作序列与最终脚本完全一致。

## 12. 阶段 8：视觉变体回归

每条测试素材至少生成：

- 1 条更换背景版本。
- 1 条更换方块资源版本。

变体视频重新识别后，以下内容必须保持不变：

- 动作数量和顺序。
- 槽位和完整形状。
- 落点。
- 消除开关和消除顺序。
- 棋盘切换时机。

只允许背景、方块资源和对应视觉效果发生变化。

## 13. 自动回归与报告

建立测试集清单和批量回归命令，每次修改识别、回放或录制代码后自动运行 T01-T06。

每条素材输出独立报告：

```text
棋盘定位：通过/失败/人工
行列规格：实际值/期望值
人工真值动作数：N
自动确认动作数：N
待确认动作数：N
识别失败动作数：N
规则重算：通过/失败步骤
客户端回放：通过/失败步骤
最大帧时间戳间隔：X 秒
60fps 输出：通过/失败
反向识别：通过/失败
背景变体：通过/失败
方块变体：通过/失败
```

汇总报告必须显示 `6/6 全部通过`；任何一条失败时，整体状态为失败。

## 14. 执行顺序

1. 建立 T01-T06 人工真值。
2. 完成多帧棋盘定位和人工行列校准。
3. 完成同色出块拆格和多帧恢复。
4. 完成四阶段证据帧。
5. 完成双向候选、约束求解和多解处理。
6. 完成校对界面、级联重算和版本管理。
7. 完成模板复用保护。
8. 完成客户端回放、反向识别和视频验收。
9. 完成背景与方块资源变体回归。
10. 运行全量回归，直到 T01-T06 五层验收全部通过。

## 15. 当前状态

| 阶段 | 状态 |
|---|---|
| 测试素材盘点 | 已完成（6条） |
| 60条数据集自动选样 | 已完成候选 v1（开发36、调优12、封存测试12）；路径、元数据、家族和感知指纹校验通过，待人工复核后锁定 |
| 标注系统规范 | 已写入第17节；待实现12条试标工作流 |
| 动作状态机优化方案 | 已写入第18节；待标注试验集建立后实施和调参 |
| 人工真值 | 识别草稿已生成；待用户逐步确认 |
| 多帧棋盘定位 | 已完成，T01-T06 自动定位 6/6 |
| 人工棋盘规格校准 | 已完成并通过接口级重算测试 |
| 同色出块拆格 | 已完成网格拟合与多帧槽位恢复 |
| 多阶段证据帧 | 已完成，放置前/后覆盖 100% |
| 双向约束求解 | 已完成唯一解与棋盘差分候选 |
| 多解人工确认 | 已完成候选保留、选择和手工编辑界面 |
| 级联重算与版本 | 已完成全链规则重算、版本保存与恢复 |
| 模板保护 | 已完成棋盘、出块轮次和越界校验 |
| 全量回放与视频验收 | 已有 12 步人工真值基准通过 dry-run；T01-T06 待人工真值确认后执行 |
| 6/6 回归通过 | 识别层 6/6；最终五层验收待人工真值与视频回放 |

## 16. 数据集治理与划分

### 16.1 第一版数据规模

第一版人工标注集固定为 60 条视频，视频不复制，仅在清单中保存绝对路径、文件指纹和数据集角色：

| 数据集 | 数量 | 用途 |
|---|---:|---|
| 开发集 | 36 | 包含 T01-T06 和 30 条新样本；用于开发、定位错误和修改算法 |
| 调优集 | 12 | 用于选择阈值、比较方案和停止调参；不能逐条编写特例 |
| 封存测试集 | 12 | 在方案和阈值冻结后一次性运行；失败后如继续修改算法，必须记录新版本并重新封存测试 |

### 16.2 内容覆盖

60 条视频按多标签覆盖以下场景；分类可以重叠，不要求各类数量相加等于 60：

- 编辑器固定棋盘约 12 条。
- 编辑器随机棋盘约 12 条。
- 连消、全消、延迟消除约 12 条。
- 竞品复刻或经典三槽拼图录屏约 10 条；不包含真人实拍和特殊活动关卡。
- 快节奏、拖拽遮挡明显约 6 条。
- 2D、3D、彩块、纯色和变色等视觉特殊样本约 6 条。
- 稀有但当前编辑器可以复现的规则或预设样本约 2 条。

### 16.3 防止数据泄漏

- 划分单位是“内容家族”，不是文件、作者或文件夹。
- 同一母版衍生出的换色、换背景、节日、片尾、分辨率和导出版本必须进入同一数据集。
- 先用规范化文件名聚类，再用时长、分辨率、文件大小和抽样帧指纹检查近重复。
- T01-T06 以及与其同家族的网络素材只能进入开发集。
- 调优集和封存测试集的路径清单一经确认即锁定；调参期间不得查看封存测试逐步结果。
- 清单生成必须可复现：保存选择脚本版本、随机种子、源目录和生成时间。

## 17. 标注系统设计

### 17.0 前端组件基线

- 通用界面组件使用 Bootstrap，播放器使用 Video.js，可拖拽/缩放动作区间使用 vis-timeline，校准截图区域框选使用 Cropper.js，图标使用 Lucide。
- 所有依赖固定版本并保存在项目 `vendor` 目录，离线可运行。
- 不自行实现播放器进度条、时间范围拖拽、缩放、选择和通用弹窗等基础能力；项目代码只负责业务数据映射、规则校验和视觉主题。
- 组件选型与例外遵循 `AGENTS.md` 的 Frontend Component Policy。

### 17.1 标注层级

**视频级标注**：

- 视频路径、指纹、分辨率、帧率、时长和数据集角色。
- 玩法有效时间范围，排除开场、片尾和非玩法画面。
- 棋盘矩形、行数、列数、单格尺寸和底部三个槽位区域。
- 棋盘是否切换、切换时间和每段使用的棋盘配置。

**动作级标注**：

- 动作序号和所属出块轮次。
- 放置前稳定区间、拾取开始帧、拖拽区间、落下帧、放置后稳定区间。
- 消除开始帧、消除结束帧；无消除时明确标记 `none`，不以空值代替。
- 来源槽位、方块二维形状、棋盘落点。
- 放置前棋盘、刚落下棋盘、消除后棋盘。
- 消除开关、完整行列、连消阶段和下一轮出块出现时间。
- 候选答案列表；确有多解时保留多个合法答案，不强造唯一真值。

### 17.2 标注工作流

1. 算法先生成预标注和候选时间区间。
2. 标注员在真实棋盘编辑器中拖动时间轴，查看放置前、拖拽、落下和消除的连续多帧证据。
3. 通过点击槽位、编辑方块格和点击棋盘落点完成校正，棋盘立即渲染放置、重叠、完整行列和消除结果。
4. 修改任一动作后，从该动作开始级联重算后续棋盘；冲突步骤必须显式显示。
5. 完成自动一致性检查后提交；高不确定样本由第二人复核。
6. 保存预标注、人工修订和审核通过三个不可覆盖版本，并记录修改人、时间和算法版本。

### 17.3 标注质量检查

- 动作时间必须单调且不能跨越下一动作。
- 一个槽位的一次消耗只能对应一个动作；三块耗尽后才允许进入下一轮。
- 方块形状必须连通、落点合法且不能与放置前棋盘重叠。
- 按标注动作模拟得到的每个稳定棋盘必须与标注棋盘一致。
- 启用消除时，完整行列及消除后棋盘必须由规则模拟复现。
- 删除、修改动作后必须级联重算，禁止保留过期的后续棋盘。
- 抽取至少 20% 样本双人复核，并统计动作边界、形状、落点和消除标签的一致率。

### 17.4 第一轮标注顺序

- 先标 T01-T06，验证标注模型和页面交互。
- 再从新增开发集选 6 条组成 12 条试标集，覆盖录屏、快节奏、连消、随机棋盘、低分辨率和特殊视觉。
- 试标完成后修订规范和工具，再标完剩余 48 条，避免带着错误规范批量返工。

## 18. 动作识别优化算法

### 18.1 从变化点升级为动作状态机

不再将每个棋盘变化直接视为一步。对连续帧计算棋盘占用、格内置信度、底部槽位生命周期、局部运动、遮挡和完整行列等信号，并按以下状态解释：

```text
IDLE（放置前稳定）
  -> PICKUP（某槽位开始消失或被拿起）
  -> DRAG（方块/手势在棋盘上移动）
  -> DROP（候选形状在某落点出现）
  -> SETTLE（放置后短暂稳定）
  -> CLEAR（可选，消除动画及连消）
  -> COMMIT（棋盘与槽位生命周期闭合，提交一个动作）
```

只有完整证据链闭合后才提交动作。拖拽覆盖、落下动画、分数特效和同一次连消产生的多个变化点必须归入同一动作窗口。

### 18.2 全窗口联合求解

- 在动作窗口内保留多张放置前、动作中、放置后和消除后帧，不再各阶段只取一张图。
- 同时枚举来源槽位、方块形状、落点和消除状态，用规则模拟解释整个时间窗口。
- 棋盘变化不能直接唯一决定方块形状时，联合槽位形状、合法落点和后续棋盘进行约束。
- 有多个合法答案时输出候选集和置信度，交给人工确认。
- 没有合法答案时输出失败原因和证据窗口，不将噪声拆成额外动作。

### 18.3 消除与出块轮次

- `SETTLE` 后继续观察到棋盘重新稳定或下一次拾取，避免把延迟消除漏掉。
- 消除检测由规则可解释性和多帧视觉证据共同决定，不能只看一张消除特效帧。
- 同一次放置引发的多段连消属于一个动作，并记录多个消除阶段。
- 三个槽位分别维护“出现、被拾取、已消耗、下一轮刷新”状态；槽位动画不能单独生成动作。

### 18.4 自适应与调参原则

- 时间阈值使用源视频帧率换算，不使用固定帧数。
- 颜色、亮度和边缘阈值以棋盘局部统计自适应，低分辨率样本单独验证。
- 开发集用于发现新信号和修复逻辑，调优集用于选择阈值。
- 阈值冻结后才运行封存测试；禁止按单个测试视频文件名增加特例。
- 每次算法版本保存参数、代码提交、数据清单和逐类指标，保证结果可追溯。

## 19. 数据集验收指标

在动作级真值建立后，报告至少包含：

| 指标 | 定义 | 第一版门槛 |
|---|---|---:|
| 动作数完全正确率 | 单条视频预测动作数与真值完全一致 | 开发集 100%，调优集 >= 95%，封存测试 >= 90% |
| 拆步/并步错误 | 一个真动作被拆成多步或多动作被并成一步 | 封存测试平均每条 <= 0.1 |
| 槽位准确率 | 来源槽位正确 | >= 98% |
| 形状完全准确率 | 方块二维格形完全一致 | >= 97% |
| 落点完全准确率 | 左上锚点行列完全一致 | >= 97% |
| 消除状态准确率 | 无消除、消除和连消阶段正确 | >= 98% |
| 动作边界误差 | 拾取、落下和稳定边界与真值的帧差 | 95% 在 3 个源帧内 |
| 棋盘单格准确率 | 每个稳定状态的占用格正确 | >= 99.5% |
| 全序列完全正确率 | 一条视频所有动作字段均正确 | 封存测试 >= 80% |

封存测试除汇总指标外，还必须报告每种场景的结果；不能用大量简单视频掩盖录屏、快节奏或连消场景失败。

## 20. 新执行顺序

1. 生成 60 条数据集清单并完成人工家族泄漏复核。
2. 实现标注数据结构、时间轴和棋盘交互，完成 12 条试标。
3. 根据试标结果冻结标注规范，完成剩余人工真值。
4. 将现有变化点算法改造成动作状态机和槽位生命周期模型。
5. 在 36 条开发集迭代，在 12 条调优集选择阈值。
6. 冻结算法与参数后运行 12 条封存测试。
7. 达不到第 19 节门槛时记录失败类别，进入下一算法版本；不得移动失败视频或改变数据集角色。

## 21. 十条真值增量优化（2026-07-20）

- 当前已确认真值：DEV-001 至 DEV-010，共 10 条视频、254 个动作。
- 新增样本 DEV-009 为主题化方块，主要暴露槽位和形状识别波动。
- 新增样本 DEV-010 为木质 3D 方块，既有短间隔动作误合并，也有棋盘占用态未变化导致的候选缺失。
- `legacy_stable_boundary_v5` 增加稳定态边界保护：相邻候选之间跨过额外稳定态且间隔不少于 0.50 秒时，禁止合并为一个动作。
- 该规则不得替代候选生成层修复。DEV-010 在 12.8s 至 14.5s 等区间存在真实动作，但木质纹理下的占用态没有形成独立稳定状态，下一轮应增强单格占用证据和槽位生命周期。
- 批量重分析结果保留各处理阶段的事件数、撤回过滤步骤和尾部过滤步骤，便于区分“未生成”和“后处理删除”。
- 本轮十条真值结果：匹配 225/254，误报 37，漏报 29，精度 85.88%，召回 88.58%。DEV-010 从 21 个匹配动作提升到 23 个，新增动作均与真值匹配。
